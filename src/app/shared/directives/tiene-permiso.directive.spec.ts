import { TienePermisoDirective } from './tiene-permiso.directive';

describe('TienePermisoDirective', () => {
  it('should create an instance', () => {
    const directive = new TienePermisoDirective();
    expect(directive).toBeTruthy();
  });
});
