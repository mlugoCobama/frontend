import { ComisionesAccesoriosComponent } from './comisiones-accesorios.component'

describe('ComisionesAccesoriosComponent', () => {
  it('should mount', () => {
    cy.mount(ComisionesAccesoriosComponent)
  })
})