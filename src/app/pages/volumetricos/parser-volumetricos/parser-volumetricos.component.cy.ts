import { ParserVolumetricosComponent } from './parser-volumetricos.component'

describe('ParserVolumetricosComponent', () => {
  it('should mount', () => {
    cy.mount(ParserVolumetricosComponent)
  })
})