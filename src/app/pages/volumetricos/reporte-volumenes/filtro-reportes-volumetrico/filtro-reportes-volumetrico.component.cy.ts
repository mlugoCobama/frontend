import { FiltroReportesVolumetricoComponent } from './filtro-reportes-volumetrico.component'

describe('FiltroReportesVolumetricoComponent', () => {
  it('should mount', () => {
    cy.mount(FiltroReportesVolumetricoComponent)
  })
})