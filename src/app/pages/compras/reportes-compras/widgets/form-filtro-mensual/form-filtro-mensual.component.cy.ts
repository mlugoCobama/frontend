import { FormFiltroMensualComponent } from './form-filtro-mensual.component'

describe('FormFiltroMensualComponent', () => {
  it('should mount', () => {
    cy.mount(FormFiltroMensualComponent)
  })
})