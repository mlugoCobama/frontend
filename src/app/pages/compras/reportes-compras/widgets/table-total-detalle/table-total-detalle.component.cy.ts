import { TableTotalDetalleComponent } from './table-total-detalle.component'

describe('TableTotalDetalleComponent', () => {
  it('should mount', () => {
    cy.mount(TableTotalDetalleComponent)
  })
})