import { ModalTokenAgenciaComponent } from './modal-token-agencia.component'

describe('ModalTokenAgenciaComponent', () => {
  it('should mount', () => {
    cy.mount(ModalTokenAgenciaComponent)
  })
})